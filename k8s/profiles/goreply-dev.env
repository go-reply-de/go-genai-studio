# goreply dev only, layered over default.env. Changes no sizing - it exists to
# turn on the self-managed TLS Secret the regional Gateway needs, without
# putting it in default.env where every other environment would inherit it.
#
# ###ENVIRONMENT### is resolved by a later pass - do not reorder cloudbuild.
